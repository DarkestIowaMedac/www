/*document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('.asiento').forEach(asiento => {
        asiento.addEventListener('click', () => {
            if (asiento.classList.contains('disponible')) {
                alert('Asiento seleccionado: ' + asiento.textContent);
                // Aquí puedes añadir la lógica para reservar el asiento
            } else {
                alert('Este asiento está ocupado');
            }
        });
    });
});*/
